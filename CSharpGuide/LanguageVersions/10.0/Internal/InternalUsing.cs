namespace CSharpGuide.LanguageVersions._10._0.Internal
{
    internal class InternalUsing
    {
        public static void Start()
        {
            WriteLine("这里不需要再次显式的添加using static System");
        }
    }
}
