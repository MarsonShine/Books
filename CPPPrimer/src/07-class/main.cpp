#include "7-3-class-otherfeature.h"

int main()
{
    Screen myScreen(5, 5, 'X');
    myScreen.move(4, 0).set('#').display(std::cout)
    cout << "\n";
    myScreen.display(cout);
    count << "\n";
    return 0;
}